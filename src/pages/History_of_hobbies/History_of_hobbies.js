import classNames from 'classnames/bind';
import style from './History_of_hobbies.module.scss';

const ctx = classNames.bind(style);

function History_of_hobbies() {
  return <div className={ctx('History_of_hobbies')}>History_of_hobbies</div>;
}

export default History_of_hobbies;
